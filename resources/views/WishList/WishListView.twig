{% extends getPartial('page-design') %}

{% block PartialHead %}
    {% embed getPartial('page-metadata') %}
        {% block title trans("Frontend::Template.wishList") %}
    {% endembed %}
{% endblock %}

{% block PageBody %}
    <div class="page-content wishlist container-max mb-5 py-3 bg-white">
        <div class="row mt-3">
            <div class="col-12">
                <h1 class="h2">{{ trans("Frontend::Template.wishList") }}</h1>
            </div>
        </div>

        <hr>

        <wish-list></wish-list>
    </div>
{% endblock %}
